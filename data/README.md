# Data Directory

Raw datasets are not included in this repository.

Place CSV files here:

- `data/raw/cicids2017/`
- `data/raw/unsw_nb15/`

The code supports multiple CSV files in each dataset folder. Processed outputs should remain local and must not be committed.
