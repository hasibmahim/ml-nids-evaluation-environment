# Results Directory

Generated experiment results are written here:

- `tables/` for CSV metrics and threshold tables
- `figures/` for PNG plots
- `metadata/` for JSON metadata

Generated results are ignored by Git because they may be large and should be reproducible from configs and data.
